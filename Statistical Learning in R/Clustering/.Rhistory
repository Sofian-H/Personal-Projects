class_indices[j+1]<-i
}
}
}
}
for (i in 0:9) {
#For obvious purposes, we're this time selecting off the original training set
show_digit(test[class_indices[i+1],1:784])
show_digit(colMeans(as.matrix(test[test$y==predictions$class[class_indices[i+1]],1:784])))
}
dim(test)
for (i in 0:9) {
#For obvious purposes, we're this time selecting off the original training set
show_digit(test[class_indices[i+1],1:784])
print(i)
show_digit(colMeans(as.matrix(test[test$y==predictions$class[class_indices[i+1]],1:784])))
}
class_indices
test$y[260]
predictions$class[260]
for (i in 0:9) {
#For obvious purposes, we're this time selecting off the original training set
show_digit(test[class_indices[i+1],1:784])
print(i)
show_digit(colMeans(as.matrix(test[test$y==predictions$class[class_indices[i+1],1:784]])))
}
for (i in 0:9) {
#For obvious purposes, we're this time selecting off the original training set
show_digit(test[class_indices[i+1],1:784])
print(i)
show_digit(colMeans(as.matrix(test[test$y==predictions$class[class_indices[i+1]],1:784])))
}
dim(predictions)
predictions
for (i in 0:9) {
#For obvious purposes, we're this time selecting off the original training set
show_digit(test[class_indices[i+1],1:784])
print(i)
show_digit(colMeans(as.matrix(test[test$y==predictions$class[class_indices[i+1]],1:784])))
}
for (i in 0:9) {
#For obvious purposes, we're this time selecting off the original training set
show_digit(test[class_indices[i+1],1:784])
print(test[test$y==predictions$class[class_indices[i+1]],1:784])
show_digit(colMeans(as.matrix(test[test$y==predictions$class[class_indices[i+1]],1:784])))
}
for (i in 0:9) {
#For obvious purposes, we're this time selecting off the original training set
show_digit(test[class_indices[i+1],1:784])
dim(test[test$y==predictions$class[class_indices[i+1]],1:784])
show_digit(colMeans(as.matrix(test[test$y==predictions$class[class_indices[i+1]],1:784])))
}
for (i in 0:9) {
#For obvious purposes, we're this time selecting off the original training set
show_digit(test[class_indices[i+1],1:784])
print(dim(test[test$y==predictions$class[class_indices[i+1]],1:784]))
show_digit(colMeans(as.matrix(test[test$y==predictions$class[class_indices[i+1]],1:784])))
}
class_indices
for (i in 0:9) {
#For obvious purposes, we're this time selecting off the original training set
show_digit(test[class_indices[i+1],1:784])
print(dim(test[test$y==predictions$class[class_indices[i+1]],1:784]))
show_digit(colMeans(as.matrix(test[test$y==predictions$class[class_indices[i+1]],1:784])))
}
for (i in 0:9) {
print(i)
#For obvious purposes, we're this time selecting off the original training set
show_digit(test[class_indices[i+1],1:784])
show_digit(colMeans(as.matrix(test[test$y==predictions$class[class_indices[i+1]],1:784])))
}
for (i in 0:9) {
print(i)
#For obvious purposes, we're this time selecting off the original training set
show_digit(test[class_indices[i+1],1:784])
print(i+10)
show_digit(colMeans(as.matrix(test[test$y==predictions$class[class_indices[i+1]],1:784])))
}
class_indices
#Question f
#The code is almost the same as in e but in the first "if" block, the first == becomes a !=
class_maxs<-(c(0:9)*0)+1.0e30 #setting it to a value which I know can't be outgrown
class_indices<-c(0:9)*0
for (i in 1:10000) {
for (j in 0:9) {
if ((proj_train$V38[i]!=new_preds$class[i]) && new_preds$class[i]==j) {
n<-norm(as.matrix(colMeans(proj_train[proj_train$V38==j,1:37])),"2")
if (class_maxs[j+1]>n) {
class_maxs[j+1]<-n
class_indices[j+1]<-i
}
}
}
}
for (i in 0:9) {
print(i)
show_digit(train[class_indices[i+1],1:784])
print(i+10)
show_digit(colMeans(as.matrix(train[train$y==new_preds$class[class_indices[i+1]],1:784])))
}
#Question f
#The code is almost the same as in e but in the first "if" block, the first == becomes a !=
class_maxs<-(c(0:9)*0)+1.0e30 #setting it to a value which I know can't be outgrown
class_indices<-c(0:9)*0
for (i in 1:10000) {
for (j in 0:9) {
if ((proj_train$V38[i]!=new_preds$class[i]) && new_preds$class[i]==j) {
n<-norm(as.matrix(colMeans(proj_train[proj_train$V38==j,1:37])),"2")
if (class_maxs[j+1]>n) {
class_maxs[j+1]<-n
class_indices[j+1]<-i
}
}
}
}
for (i in 0:9) {
show_digit(train[class_indices[i+1],1:784])
show_digit(colMeans(as.matrix(train[train$y==new_preds$class[class_indices[i+1]],1:784])))
}
class_indices
train$y[81]
new_preds$class[81]
#Question e
class_mins<-c(0:9)*0
class_mins<-class_mins+1.000000e+30 #setting it to a value which I know can't be outgrown
class_indices<-c(0:9)*0
for (i in 1:60000) {
for (j in 0:9) {
if ((proj_train$V38[i]==new_preds$class[i]) && new_preds$class[i]==j) {
n<-norm(as.matrix(colMeans(proj_train[proj_train$V38==j,1:37]))-
proj_train[i,1:784],"2")
if (class_mins[j+1]>n) {
class_mins[j+1]<-n
class_indices[j+1]<-i
}
}
}
}
#Question e
class_mins<-c(0:9)*0
class_mins<-class_mins+1.000000e+30 #setting it to a value which I know can't be outgrown
class_indices<-c(0:9)*0
for (i in 1:60000) {
for (j in 0:9) {
if ((proj_train$V38[i]==new_preds$class[i]) && new_preds$class[i]==j) {
n<-norm(as.matrix(colMeans(proj_train[proj_train$V38==j,1:37]))-
proj_train[i,1:37],"2")
if (class_mins[j+1]>n) {
class_mins[j+1]<-n
class_indices[j+1]<-i
}
}
}
}
for (i in 0:9) {
#For obvious purposes, we're this time selecting off the original training set
show_digit(train[class_indices[i+1],1:784])
show_digit(colMeans(as.matrix(train[train$y==i,1:784])))
}
#Question f
#The code is almost the same as in e but in the first "if" block, the first == becomes a !=
class_maxs<-(c(0:9)*0)+1.0e30 #setting it to a value which I know can't be outgrown
class_indices<-c(0:9)*0
for (i in 1:60000) {
for (j in 0:9) {
if ((proj_train$V38[i]!=new_preds$class[i]) && new_preds$class[i]==j) {
n<-norm(as.matrix(colMeans(proj_train[proj_train$V38==j,1:37]))
-proj_train[i,37],"2")
if (class_maxs[j+1]>n) {
class_maxs[j+1]<-n
class_indices[j+1]<-i
}
}
}
}
for (i in 0:9) {
show_digit(train[class_indices[i+1],1:784])
show_digit(colMeans(as.matrix(train[train$y==new_preds$class[class_indices[i+1]],1:784])))
}
#Question f
#The code is almost the same as in e but in the first "if" block, the first == becomes a !=
class_maxs<-(c(0:9)*0)+1.0e30 #setting it to a value which I know can't be outgrown
class_indices<-c(0:9)*0
for (i in 1:60000) {
for (j in 0:9) {
if ((proj_train$V38[i]!=new_preds$class[i]) && new_preds$class[i]==j) {
n<-norm(as.matrix(colMeans(proj_train[proj_train$V38==j,1:37]))
-proj_train[i,37],"2")
if (class_maxs[j+1]>n) {
class_maxs[j+1]<-n
class_indices[j+1]<-i
}
}
}
}
for (i in 0:9) {
show_digit(train[class_indices[i+1],1:784])
show_digit(colMeans(as.matrix(train[train$y==new_preds$class[class_indices[i+1]],1:784])))
}
class_indices
train$y[3815]
new_preds$class[3815]
for (i in 0:9) {
show_digit(train[class_indices[i+1],1:784])
show_digit(colMeans(as.matrix(train[train$y==new_preds$class[class_indices[i+1]],1:784])))
}
train$y[43310]
new_preds$class[43310]
class_indices
train$y[28393]
new_preds$class[28393]
train$y[class_indices[2]]
new_preds$class[class_indices[2]]
class_indices[2]
for (i in 0:9) {
show_digit(train[class_indices[i+1],1:784])
show_digit(colMeans(as.matrix(train[train$y==new_preds$class[class_indices[i+1]],1:784])))
print(paste(train$y[class_indices[i+1]],",",new_preds$class[class_indices[i+1]]))
}
#Question e
class_mins<-c(0:9)*0
class_mins<-class_mins+1.000000e+30 #setting it to a value which I know can't be outgrown
class_indices<-c(0:9)*0
for (i in 1:60000) {
for (j in 0:9) {
if ((proj_test$V38[i]==knn_model[i]) && knn_model[i]==j) {
n<-norm(as.matrix(colMeans(proj_test[proj_test$V38==j,1:37]))
-proj_test[i,37],"2")
if (class_mins[j+1]>n) {
class_mins[j+1]<-n
class_indices[j+1]<-i
}
}
}
}
for (i in 0:9) {
#For obvious purposes, we're this time selecting off the original training set
show_digit(test[class_indices[i+1],1:784])
show_digit(colMeans(as.matrix(test[test$y==i,1:784])))
}
if ((proj_test$V38[i]==knn_model[i]) && knn_model[i]==j) {
n<-norm(as.matrix(colMeans(proj_test[proj_test$V38==j,1:37]))
-proj_test[i,37],"2")
if (class_mins[j+1]>n) {
class_mins[j+1]<-n
class_indices[j+1]<-i
}
}
for (i in 0:9) {
#For obvious purposes, we're this time selecting off the original training set
show_digit(test[class_indices[i+1],1:784])
show_digit(colMeans(as.matrix(test[test$y==i,1:784])))
}
knn_model
#Question f
#The code is almost the same as in e but in the first "if" block, the first == becomes a !=
class_maxs<-(c(0:9)*0)+1.0e30 #setting it to a value which I know can't be outgrown
class_indices<-c(0:9)*0
for (i in 1:10000) {
for (j in 0:9) {
if ((proj_train$V38[i]!=knn_model[i]) && knn_model[i]==j) {
n<-norm(as.matrix(colMeans(proj_test[proj_test$V38==j,1:37]))
-proj_test[i,37],"2")
if (class_maxs[j+1]>n) {
class_maxs[j+1]<-n
class_indices[j+1]<-i
}
}
}
}
for (i in 0:9) {
show_digit(test[class_indices[i+1],1:784])
show_digit(colMeans(as.matrix(test[test$y==knn_model[class_indices[i+1]],1:784])))
print(paste(test$y[class_indices[i+1]],",",knn_model[class_indices[i+1]]))
}
########################################################################################################################
#Question f
#The code is almost the same as in e but in the first "if" block, the first == becomes a !=
class_maxs<-(c(0:9)*0)+1.0e30 #setting it to a value which I know can't be outgrown
class_indices<-c(0:9)*0
for (i in 1:60000) {
for (j in 0:9) {
if ((proj_train$V38[i]!=new_preds$class[i]) && new_preds$class[i]==j) {
n<-norm(as.matrix(colMeans(proj_train[proj_train$V38==j,1:37]))
-proj_train[i,1:37],"2")
if (class_maxs[j+1]>n) {
class_maxs[j+1]<-n
class_indices[j+1]<-i
}
}
}
}
for (i in 0:9) {
show_digit(train[class_indices[i+1],1:784])
show_digit(colMeans(as.matrix(train[train$y==new_preds$class[class_indices[i+1]],1:784])))
print(paste(train$y[class_indices[i+1]],",",new_preds$class[class_indices[i+1]]))
}
########################################################################################################################
#Question f
#The code is almost the same as in e but in the first "if" block, the first == becomes a !=
class_maxs<-(c(0:9)*0)+1.0e30 #setting it to a value which I know can't be outgrown
class_indices<-c(0:9)*0
for (i in 1:60000) {
for (j in 0:9) {
if ((proj_train$V38[i]!=new_preds$class[i]) && new_preds$class[i]==j) {
n<-norm(as.matrix(colMeans(proj_train[proj_train$V38==j,1:37]))
-proj_train[i,1:37],"2")
if (class_maxs[j+1]>n) {
class_maxs[j+1]<-n
class_indices[j+1]<-i
}
}
}
}
for (i in 0:9) {
show_digit(train[class_indices[i+1],1:784])
show_digit(colMeans(as.matrix(train[train$y==new_preds$class[class_indices[i+1]],1:784])))
print(paste(train$y[class_indices[i+1]],",",new_preds$class[class_indices[i+1]]))
}
#Question e
class_mins<-c(0:9)*0
class_mins<-class_mins+1.000000e+30 #setting it to a value which I know can't be outgrown
class_indices<-c(0:9)*0
for (i in 1:60000) {
for (j in 0:9) {
if ((proj_test$V38[i]==knn_model[i]) && knn_model[i]==j) {
n<-norm(as.matrix(colMeans(proj_test[proj_test$V38==j,1:37]))
-proj_test[i,1:37],"2")
if (class_mins[j+1]>n) {
class_mins[j+1]<-n
class_indices[j+1]<-i
}
}
}
}
for (i in 0:9) {
#For obvious purposes, we're this time selecting off the original training set
show_digit(test[class_indices[i+1],1:784])
show_digit(colMeans(as.matrix(test[test$y==i,1:784])))
}
#Question e
class_mins<-c(0:9)*0
class_mins<-class_mins+1.000000e+30 #setting it to a value which I know can't be outgrown
class_indices<-c(0:9)*0
for (i in 1:60000) {
for (j in 0:9) {
if ((proj_test$V38[i]==knn_model[i]) && knn_model[i]==j) {
n<-norm(as.matrix(colMeans(proj_test[proj_test$V38==j,1:37]))
-proj_test[i,1:37],"2")
if (class_mins[j+1]>n) {
class_mins[j+1]<-n
class_indices[j+1]<-i
}
}
}
}
for (i in 0:9) {
#For obvious purposes, we're this time selecting off the original training set
show_digit(test[class_indices[i+1],1:784])
show_digit(colMeans(as.matrix(test[test$y==i,1:784])))
}
#Question e
class_mins<-c(0:9)*0
class_mins<-class_mins+1.000000e+30 #setting it to a value which I know can't be outgrown
class_indices<-c(0:9)*0
for (i in 1:10000) {
for (j in 0:9) {
if ((proj_test$V38[i]==knn_model[i]) && knn_model[i]==j) {
n<-norm(as.matrix(colMeans(proj_test[proj_test$V38==j,1:37]))
-proj_test[i,1:37],"2")
if (class_mins[j+1]>n) {
class_mins[j+1]<-n
class_indices[j+1]<-i
}
}
}
}
for (i in 0:9) {
#For obvious purposes, we're this time selecting off the original training set
show_digit(test[class_indices[i+1],1:784])
show_digit(colMeans(as.matrix(test[test$y==i,1:784])))
}
########################################################################################################################
#Question f
#The code is almost the same as in e but in the first "if" block, the first == becomes a !=
class_maxs<-(c(0:9)*0)+1.0e30 #setting it to a value which I know can't be outgrown
class_indices<-c(0:9)*0
for (i in 1:10000) {
for (j in 0:9) {
if ((proj_train$V38[i]!=knn_model[i]) && knn_model[i]==j) {
n<-norm(as.matrix(colMeans(proj_test[proj_test$V38==j,1:37]))
-proj_test[i,37],"2")
if (class_maxs[j+1]>n) {
class_maxs[j+1]<-n
class_indices[j+1]<-i
}
}
}
}
for (i in 0:9) {
show_digit(test[class_indices[i+1],1:784])
show_digit(colMeans(as.matrix(test[test$y==knn_model[class_indices[i+1]],1:784])))
print(paste(test$y[class_indices[i+1]],",",knn_model[class_indices[i+1]]))
}
class_indices
test$y[9240]
knn_model[9240]
#Question f
#The code is almost the same as in e but in the first "if" block, the first == becomes a !=
class_maxs<-(c(0:9)*0)+1.0e30 #setting it to a value which I know can't be outgrown
class_indices<-c(0:9)*0
for (i in 1:10000) {
for (j in 0:9) {
if ((proj_test$V38[i]!=knn_model[i]) && knn_model[i]==j) {
n<-norm(as.matrix(colMeans(proj_test[proj_test$V38==j,1:37]))
-proj_test[i,37],"2")
if (class_maxs[j+1]>n) {
class_maxs[j+1]<-n
class_indices[j+1]<-i
}
}
}
}
for (i in 0:9) {
show_digit(test[class_indices[i+1],1:784])
show_digit(colMeans(as.matrix(test[test$y==knn_model[class_indices[i+1]],1:784])))
print(paste(test$y[class_indices[i+1]],",",knn_model[class_indices[i+1]]))
}
#Question f
#The code is almost the same as in e but in the first "if" block, the first == becomes a !=
class_maxs<-(c(0:9)*0)+1.0e30 #setting it to a value which I know can't be outgrown
class_indices<-c(0:9)*0
for (i in 1:10000) {
for (j in 0:9) {
if ((proj_test$V38[i]!=knn_model[i]) && knn_model[i]==j) {
n<-norm(as.matrix(colMeans(proj_test[proj_test$V38==j,1:37]))
-proj_test[i,37],"2")
if (class_maxs[j+1]>n) {
class_maxs[j+1]<-n
class_indices[j+1]<-i
}
}
}
}
for (i in 0:9) {
show_digit(test[class_indices[i+1],1:784])
show_digit(colMeans(as.matrix(test[test$y==knn_model[class_indices[i+1]],1:784])))
print(paste(test$y[class_indices[i+1]],",",knn_model[class_indices[i+1]]))
}
#Question g
one_mean<-c(1:784)*0
seven_mean<-c(1:784)*0
for (i in 1:60000) {
if (train$y[i]==1 && new_preds$class[i]==1)
one_mean<-one_mean+train[i,1:784]
if (train$y[i]==7 && new_preds$class[i]==7)
seven_mean<-seven_mean+train[i,1:784]
}
one_mean<-one_mean/60000
seven_mean<-seven_mean/60000
show_digit(one_mean)
show_digit(seven_mean)
abs(-3)
abs(c(-1,1))
show_digit(abs(seven_mean-one_mean))
show_digit(seven_mean-one_mean)
#########################################################################################################################
#Question g
one_mean<-c(1:784)*0
seven_mean<-c(1:784)*0
for (i in 1:10000) {
if (test$y[i]==1 && knn_model[i]==1)
one_mean<-one_mean+train[i,1:784]
if (train$y[i]==7 && knn_model[i]==7)
seven_mean<-seven_mean+train[i,1:784]
}
one_mean<-one_mean/10000
seven_mean<-seven_mean/10000
show_digit(one_mean)
show_digit(seven_mean)
show_digit(seven_mean-one_mean)
#########################################################################################################################
#Question g
one_mean<-c(1:784)*0
seven_mean<-c(1:784)*0
for (i in 1:10000) {
if (test$y[i]==1 && knn_model[i]==1)
one_mean<-one_mean+test[i,1:784]
if (train$y[i]==7 && knn_model[i]==7)
seven_mean<-seven_mean+test[i,1:784]
}
one_mean<-one_mean/10000
seven_mean<-seven_mean/10000
show_digit(one_mean)
show_digit(seven_mean)
show_digit(seven_mean-one_mean)
show_digit(abs(seven_mean-one_mean))
#########################################################################################################################
#Question g
one_mean<-c(1:784)*0
seven_mean<-c(1:784)*0
for (i in 1:10000) {
if (test$y[i]==1 && knn_model[i]==1)
one_mean<-one_mean+test[i,1:784]
if (test$y[i]==7 && knn_model[i]==7)
seven_mean<-seven_mean+test[i,1:784]
}
one_mean<-one_mean/10000
seven_mean<-seven_mean/10000
show_digit(one_mean)
show_digit(seven_mean)
show_digit(abs(seven_mean-one_mean))
